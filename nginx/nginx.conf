events {}

http {

  upstream backend_servers {
    least_conn;
    server backend:5000;
  }

  server {
    listen 80;

    location / {
      proxy_pass http://frontend:80;
    }

    location /api/ {
      proxy_pass http://backend_servers;
    }
  }
}